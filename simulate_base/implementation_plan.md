# 實作計劃

**制定日期**: 2025-11-04  
**預估總時間**: 2-3 週

---

## 🎯 總體目標

建立完整的流量模擬系統，將真實流量轉換為 PQC-TLS 流量資料集。

---

## 📊 實作階段

### Phase 1: 基礎建設（3-4 天）

#### 目標
建立核心模組的骨架和配置系統

#### 工作項目
- [ ] 建立 `simulation_config.yaml`
- [ ] 實作 `source_manager.py`（基礎版）
- [ ] 建立測試 `tests/test_source_manager.py`
- [ ] 實作 `simulation_client.py`（包裝）
- [ ] 實作 `simulation_server.py`（包裝）

#### 驗收標準
- ✅ 能載入 JSON 特徵
- ✅ 能包裝 normal_client/server
- ✅ 通過單元測試

#### 測試方式
```python
# tests/test_source_manager.py
def test_load_json():
    mgr = SourceManager(config)
    features = mgr.get_features()
    assert 'packet_sizes' in features

# tests/test_simulation_wrapper.py
def test_client_wrapper():
    client = SimulationClient(host='localhost', port=8443)
    client.connect_silent(message="test")
    # 應該無輸出，但連線成功
```

---

### Phase 2: 模擬引擎（4-5 天）

#### 目標
實作核心的流量模擬邏輯

#### 工作項目
- [ ] 實作 `traffic_simulator.py`（單執行緒版）
- [ ] 實作調度邏輯（根據特徵生成連線）
- [ ] 建立測試 `tests/test_simulator.py`
- [ ] 實作多執行緒版本
- [ ] 實作時序控制（間隔、持續時間）

#### 驗收標準
- ✅ 能根據 JSON 特徵模擬流量
- ✅ 封包大小分布接近原始流量
- ✅ 時間間隔分布接近原始流量
- ✅ 多執行緒穩定運作

#### 測試方式
```python
# tests/test_simulator.py
def test_single_source():
    config = {...}
    simulator = TrafficSimulator(config)
    simulator.run()
    # 檢查是否完成

def test_feature_matching():
    # 比較模擬流量和來源特徵的相似度
    similarity = compare_features(original, simulated)
    assert similarity > 0.8  # 80% 相似度
```

---

### Phase 3: 資料集建構（2-3 天）

#### 目標
整合捕獲和儲存功能

#### 工作項目
- [ ] 實作 `dataset_builder.py`
- [ ] 整合 `traffic_capture.py`
- [ ] 實作 metadata 生成
- [ ] 建立檔案組織邏輯
- [ ] 建立測試 `tests/test_dataset_builder.py`

#### 驗收標準
- ✅ 能自動捕獲模擬流量
- ✅ PCAP 檔案正確儲存
- ✅ metadata.json 包含完整資訊
- ✅ 檔案結構清晰

#### 測試方式
```python
# tests/test_dataset_builder.py
def test_capture_and_save():
    builder = DatasetBuilder(config)
    builder.start_capture()
    # 執行一些流量
    builder.stop_and_save()
    
    assert os.path.exists(builder.output_dir)
    assert os.path.exists(f"{builder.output_dir}/traffic.pcap")
    assert os.path.exists(f"{builder.output_dir}/metadata.json")
```

---

### Phase 4: 整合測試（2-3 天）

#### 目標
端到端測試整個系統

#### 工作項目
- [ ] 實作 `run_simulation.py`
- [ ] 進行完整流程測試
- [ ] 修正發現的問題
- [ ] 效能優化
- [ ] 建立整合測試 `tests/test_integration.py`

#### 驗收標準
- ✅ 完整流程無錯誤
- ✅ 產出的 PCAP 可用 Wireshark 開啟
- ✅ 模擬 5 個 Client × 10 連線穩定運作
- ✅ 記憶體和 CPU 使用合理

#### 測試方式
```python
# tests/test_integration.py
def test_full_pipeline():
    # 1. 準備配置
    # 2. 執行 run_simulation.py
    # 3. 檢查產出
    # 4. 驗證 PCAP 內容
```

---

### Phase 5: 分析器（2-3 天）

#### 目標
實作事後分析功能

#### 工作項目
- [ ] 實作 `analyzer.py`
- [ ] 統計分析（封包數、大小分布）
- [ ] 相似度比較（與來源對比）
- [ ] 報告生成（文字或圖表）
- [ ] 實作 `analyze_dataset.py`

#### 驗收標準
- ✅ 能分析產出的 PCAP
- ✅ 能與原始來源比較
- ✅ 產生可讀的報告

#### 測試方式
```python
# tests/test_analyzer.py
def test_analyze():
    analyzer = DatasetAnalyzer()
    report = analyzer.analyze('datasets/sim_test_001')
    
    assert 'total_packets' in report
    assert 'similarity_score' in report
```

---

## 🧪 測試策略

### 單元測試
- 每個模組獨立測試
- 覆蓋核心邏輯
- 快速執行（< 5 秒）

### 整合測試
- 測試模組間互動
- 使用小規模配置（1 client, 5 connections）
- 中等執行時間（< 30 秒）

### 端到端測試
- 完整流程測試
- 使用實際配置
- 可接受較長執行時間（< 5 分鐘）

---

## 📅 時間規劃

| 階段 | 預估時間 | 累計時間 |
|------|---------|---------|
| Phase 1 | 3-4 天 | 3-4 天 |
| Phase 2 | 4-5 天 | 7-9 天 |
| Phase 3 | 2-3 天 | 9-12 天 |
| Phase 4 | 2-3 天 | 11-15 天 |
| Phase 5 | 2-3 天 | 13-18 天 |
| **總計** | **2-3 週** | - |

---

## ✅ 里程碑

### Milestone 1: 基礎完成（第 1 週末）
- ✅ source_manager 可用
- ✅ simulation wrapper 可用
- ✅ 所有單元測試通過

### Milestone 2: 核心完成（第 2 週末）
- ✅ 能執行基本模擬
- ✅ 能捕獲並儲存 PCAP
- ✅ 整合測試通過

### Milestone 3: 系統完成（第 3 週末）
- ✅ 分析功能完成
- ✅ 所有測試通過
- ✅ 文檔完整

---

## 🔄 開發流程

### 每個模組的開發順序
```
1. 設計介面（API）
   ↓
2. 寫測試（TDD）
   ↓
3. 實作功能
   ↓
4. 執行測試
   ↓
5. 重構優化
   ↓
6. 文檔更新
```

### 每日流程
```
早上：
  - 確認今日目標
  - 寫測試 case

下午：
  - 實作功能
  - 執行測試

晚上：
  - Code review
  - 更新文檔
```

---

## 📝 進度追蹤

使用此文檔追蹤進度，每完成一項勾選 ✅

當前階段：**Phase 0 (規劃完成，準備開始)**

---

## 🐛 已知問題

（記錄開發過程中發現的問題）

---

## 💡 改進想法

（記錄未來可能的優化方向）

---

**最後更新**: 2025-11-04